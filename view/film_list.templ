package view

import (
    "github.com/bangau1/golang-htmx/film"
)

templ FilmList(films []film.Film) {
<div class="row">
    for _, film := range films {
      <div class="col-md-4">
      @FilmTile(film)
      </div>
    }
</div>
}
